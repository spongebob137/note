## 建立保持时间

![alt text](image-1.png)

### 建立时间

- **setup**：UFF1 时钟沿到达前，信号需要保持的时间。
- 约束：T<sub>launch</sub> + T<sub>c-q</sub> + T<sub>comb</sub> + T<sub>setup</sub> <= T<sub>capture</sub> + T<sub>clk</sub>（蓝色的时间加上周期 T 要大于红色的时间。红色路径的信号需要在下一个上升沿到达前早到一个 setup 时间。）
- 修复违例：增大周期、加快红色路线的传输速度。

### 保持时间

- **hold**：UFF1 时钟沿到达后信号仍需要保持的时间。
- 约束：T<sub>launch</sub> + T<sub>c-q</sub> + T<sub>comb</sub> >= T<sub>capture</sub> + T<sub>hold</sub>（红色路径传递多于蓝色路径的时间应该大于一个 hold 时间）
- 修复违例：增大红色路线的延迟。

时序图：

![alt text](image.png)

## RTL到门级网表（综合）

- 综合：在工艺库和设计约束下，把RTL转换成门级网表，并对性能、功耗和面积进行优化。

### 综合的三个步骤

1. Translation（转译）：读取并解析RTL，把加法、选择、寄存等行为转换成GTECH通用逻辑。
2. Optimization（优化）：在保证功能一致的前提下，进行逻辑化简、删除冗余逻辑和结构调整。所以，综合不是把每一行RTL机械地换成一个逻辑门，而是在保证功能等价的前提下重新组织电路（因此RTL代码和最终网表通常无法逐行对应）。
3. Mapping（工艺映射）：根据LIB/DB，从目标工艺库中选择实际标准单元，生成Mapped Netlist。



### GTECH Netlist和Mapped Netlist

- GTECH Netlist：综合工具生成的中间网表，使用与具体工艺无关的通用逻辑（还不是后端使用的最终标准单元网表）。
- Mapped Netlist：根据目标工艺库映射后的门级网表，已经确定实际标准单元及连接关系（但还没有确定单元的物理位置和金属走线）。
- 疑问：为什么PPT说综合主要看Setup，不处理Hold，然后在cts部分处理

### 传统逻辑综合和物理综合

- 传统逻辑综合：主要根据RTL、LIB和SDC进行逻辑优化，没有真实的单元位置和布线信息（导线延迟只能忽略或粗略估算，结果容易偏乐观）。
- 物理综合：加入Floorplan、Macro、Port及工艺物理信息，能够预估单元位置、布线距离和RC延迟（综合结果更接近实际后端实现）。

## DFT扫描链和MBIST

- DFT:可测试性设计,芯片设计时，为芯片加入的专用测试硬件，如扫描链和MBIST（核心目标：可控制性，能把内部节点设置成需要的0或1；可观测性，能把内部节点的结果传到芯片外部）
    1.  扫描链： 将普通D触发器换为带scan信号的D触发器，再串成扫描链（检查芯片内特定寄存器的输入输出是否正常，扫描链方便数据的写入和读出）
        （后端需要关心扫描链，是因为Scan DFF和扫描链连线会影响面积、时序和布线，place阶段还需要按物理距离进行扫描链重组）
    2.  MBIST：存储器内建自测试，通过片上控制电路自动对Memory进行写入、读出和结果比较（Memory内部每个存储单元不会像普通DFF一样暴露在门级网表中，不能直接用扫描链逐个测试）
- 两者在流程中的位置和区别：MBIST相关RTL通常与功能RTL一起综合，扫描链通常在综合后对门级网表插入；扫描链主要测试寄存器和组合逻辑，MBIST主要测试Memory。






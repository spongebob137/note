# 2026-07-31 重点学习进度

## 总结

本轮不再按教程从头到尾学习，只完成会议明确要求的三部分：Tcl 核心语法及 EDA 应用、LSF 常用命令、Makefile 基本依赖关系。学习方式保持为一次一个实操任务，完成并核对输出后再进入下一项。

## 今日完成标准

- 能独立使用 Tcl 变量和 List，并写出 `if`、`foreach` 和简单 `proc`。
- 能从 EDA collection 中遍历对象，读取名称、类型或 `bbox`，并用 `lindex` 拆坐标。
- 能说明并实际使用 `bsub`、`bjobs`、`bkill`，能读懂 `bqueues`、`bhosts`、`lsload` 的基本输出。
- 能读懂 Makefile 中的目标、依赖和命令，并解释一条后端流程依赖链的执行顺序。

## 学习进度

| 模块 | 会议重点 | 状态 | 完成判据 |
|---|---|---|---|
| Tcl 变量 | `set`、读取、修改 | 已完成 | 能区分变量名 `training` 与变量值 `$training` |
| Tcl List | 教程第 18-29 页 | 已完成 | 能构造、追加、索引、计数、搜索和排序 |
| Tcl 控制流 | `if`、`while`、`for`、`foreach`、`break`、`continue` | 已完成 | 独立完成列表分类与循环控制任务 |
| Tcl 过程 | `proc`、参数、返回值、局部与全局变量 | 已完成 | 独立封装一个可复用的数据处理过程 |
| Tcl 字符串 | 重点掌握 `string compare`，补充常用子命令 | 已完成 | 能进行比较、匹配和提取 |
| Tcl EDA 应用 | collection、属性、`bbox` 坐标 | 已完成 | 遍历所选 cell 并输出名称、类型与坐标 |
| LSF | `bsub`、`bjobs`、`bkill`；`bqueues`、`bhosts`、`lsload` | 进行中 | 能提交、观察和终止一个安全的小任务 |
| Makefile | 目标、依赖、具体命令 | 未开始 | 能读懂并运行一条三阶段依赖链 |

## 本轮范围

### Tcl

主要参考 `021_Tcl教程.pdf`：List 第 18-29 页、控制流第 30-35 页、过程第 36-39 页，以及字符串命令相关页面。本轮不继续学习文件访问、异常处理和深入 Tcl 等非会议重点章节。

### LSF

先掌握三条工作主线：

1. `bsub` 提交任务。
2. `bjobs` 查看任务及 `PEND`、`RUN`、`SUSP` 等状态。
3. `bkill` 按 Job ID 终止异常任务。

`bqueues`、`bhosts`、`lsload` 用于提交前查看队列、主机和负载，只要求会查询和识别关键字段。

### Makefile

核心规则：

```make
target: prerequisites
	command
```

- `target`：要生成的文件或要执行的任务名。
- `prerequisites`：执行该目标前必须满足的文件或其他目标。
- `command`：构建目标时执行的 Shell 命令，行首通常必须是 Tab。

重点理解后端流程中 `initial -> floorplan -> place -> cts -> route -> signoff` 一类依赖链如何决定执行顺序，不要求深入 Makefile 函数、模式规则等高级语法。

## 资料

- Tcl：`C:\Users\17381\Documents\BD\工具手册\tcl\021_Tcl教程.pdf`
- Makefile：`C:\Users\17381\Documents\BD\工具手册\makefile`
- EDA 命令参考：`C:\Users\17381\Documents\BD\md资料\工具手册-fusion_compiler_user_guide_2021.06.md`
- 学习问题：`C:\Users\17381\Documents\BD\note\学习问题`

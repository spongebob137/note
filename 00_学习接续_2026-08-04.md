# 学习接续卡（2026-08-04）

## 总结

当前主线已由基础工具学习切换到 **FC/CFlow 数字后端实操**。现阶段已经执行完 `make Ic2Initial`，下一步不是立即盲跑，而是先检查 Initial 产物和实际 Makefile 目标，再把老师提供的 `fp.tcl` 配置到 `vars(PR,FLOORPLAN_TCL)`，随后进入 Floorplan。

前面未完成的 Makefile 等内容保留进度，暂不丢失；后续结合真实 CFlow Makefile 补学，会更容易理解。

## 当前进度

| 模块 | 当前状态 | 接续位置 |
|---|---|---|
| Linux/Vim | 基础内容已完成并做过日志实操 | 后续随 FC 实操巩固检索、复制、编辑和权限判断 |
| Tcl | 会议要求的变量、List、控制流、proc、字符串及 FC collection 基础已学习 | 后续在真实 FC 脚本中继续练习 |
| LSF | `bsub`、`bjobs`、`bkill`、`bqueues`、`bhosts`、`lsload` 已学习并复习 | 后续结合 CFlow 任务监控巩固 |
| Makefile | 已开始目标、依赖、recipe；尚未系统学习时间戳和多级依赖 | **暂停**，结合 CFlow 的实际 Makefile 再继续 |
| FC/CFlow | **当前主线** | `make Ic2Initial` 已完成，准备 Floorplan |

## 当前实操上下文

- 学习 PPT：`C:\Users\17381\Downloads\cflow_quick_start(1).pptx`
- FC 本地资料：`C:\Users\17381\Documents\BD\工具手册\fc`
- 数字 IC 本地资料：`C:\Users\17381\Documents\BD\md资料`
- 老师提供的 Floorplan 脚本：`/prj/yundu_u/users/xinyuyang_sd/pr_runs/try_run0/fp.tcl`
- 待配置变量：`vars(PR,FLOORPLAN_TCL)`
- Training 输入：`/prj/yundu_u/users/jingweizhang_sd/impl_tarining/input`
- rm_flow 参考：`/prj/yundu_u/users/jingweizhang_sd/impl_tarining/rm_flow`
- Training library：`SAMSUNG_S4 7P94T`

## 学习方式

1. **一次只推进一个可验证任务。** 每次给出目的、少量必要提示、完成标准；实操者执行后贴出原始命令和输出，再进入下一步。
2. **命令前先弄清六件事。** 目的、输入、输出、配置来源、风险、成功标志。
3. **证据优先。** 每个阶段保留配置差异、命令、log、flag、数据库、report、运行时间、内存和检查结论。
4. **总结放最前。** 笔记用 `【重点】`、`【常用】`、`【易错】`、`【了解】` 标记重要程度。
5. **问题分类记录。** 概念问题写入对应知识笔记；真实故障写入 `学习问题`；简单拼写错误不单独收集。
6. **参考目录只读。** 老师、同事及公共目录中的脚本先查看、记录来源，再复制到个人运行目录或分支修改。
7. **一次只改变一个关键变量。** 面积和频率实验都以 Base run 为基线，否则无法判断 QoR 变化由什么引起。

## 信息来源优先级

1. 老师针对当前项目给出的明确要求。
2. 当前运行目录真实生成的 Makefile、配置、log、report 和 FC `help`/`man`。
3. `cflow_quick_start(1).pptx`。
4. 当前版本 Fusion Compiler User Guide 和本地数字 IC 资料。
5. 微盘资料；当前分享页需要登录，取得内容后再纳入，不能凭标题推断。

## 下一次对话从这里开始

只做 **Floorplan 前检查**：确认当前 run 目录、`Ic2Initial` 的 flag/log/db，并从实际 Makefile 确认下一目标究竟叫 `Ic2Floorplan` 还是 `InFloorplan`。检查通过后，下一小步才是查看并配置 `FLOORPLAN_TCL`。

